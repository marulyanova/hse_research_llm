# hse_research_llm
Репозиторий для ДЗ по курсу ВШЭ " Исследовательские задачи в области больших языковых моделей"

⚙️ 2. Формальное описание среды

🔹 Состояние (state)

Вектор наблюдения obs длиной 4:

x — позиция тележки по горизонтали (в метрах).

x_dot — скорость тележки (в м/с).

theta — угол шеста относительно вертикали (в радианах).

theta_dot — угловая скорость шеста (в рад/с).

Пример состояния:

obs = [x, x_dot, theta, theta_dot] = [0.03, -0.02, 0.05, 0.1]

🔹 Действия (actions)

Пространство действий дискретное:

0 — толкнуть тележку влево

1 — толкнуть тележку вправо


🔹 Вознаграждение (reward)

На каждом шаге:

агент получает +1 награду за каждый timestep, пока шест не упал и тележка не вышла за пределы.

Эпизод заканчивается, если:

|x| > 2.4 (тележка выехала за пределы трека)

|theta| > 12° ≈ 0.209 рад (шест упал)

либо прошло 500 шагов (в CartPole-v1), что считается «идеальным решением».

🔹 Оптимум

Максимальная возможная награда:

500 баллов за эпизод

Это значит, что агент удерживает шест вертикально 500 шагов подряд (после чего эпизод завершается автоматически).

Если агент стабильно достигает 475–500, считается, что он решил задачу.